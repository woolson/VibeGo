build/
*.dmg
.DS_Store
node_modules/
*.vsix

# Sparkle framework is fetched at build time (see build.sh), not committed.
vendor/

# Private maintainer notes (kept out of the public repo)
CLAUDE.md
ROADMAP.md
.sparkle-private-ed-key
sparkle-private-ed-key
